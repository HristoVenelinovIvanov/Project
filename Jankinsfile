
pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        bat 'mvn package'
        bat 'git status'
        bat 'git add .'
        bat 'git commit -m "Push from Jenkinsfile"'
        bat 'git push'
        bat 'git status'
      }
    }
  }
}