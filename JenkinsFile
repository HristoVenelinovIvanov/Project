@Library('my-shared-library@master') _


library 'my-shared-library@master'
pipeline {
	agent { label 'master' }

	stages {
		stage('Example stage') {
			steps {
				echo "Stage is running"
			}
		}
	}
}
